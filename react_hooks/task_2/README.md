# Task 3: Update the NotificationItem Component

## Description
This task involves converting the `NotificationItem` component from a class-based component to a functional component. We also replace `PureComponent` with `React.memo` to maintain performance optimizations.

## Tasks

### 3. Update the NotificationItem component
- Convert `NotificationItem.jsx` to a functional component.
- Use `React.memo` for memoization.
- Maintain existing prop structures and rendering behavior.
- Ensure the component only re-renders when notification items update.

## Files
- `dashboard/src/Notifications/NotificationItem.jsx`
- `dashboard/src/Notifications/NotificationItem.spec.js`
